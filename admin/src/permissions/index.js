const permissions = {
  translate: [{ action: 'plugin::strapi-plugin-fluentc.translate', subject: null }],
  usage: [{ action: 'plugin::strapi-plugin-fluentc.usage', subject: null }],
}

export default permissions
